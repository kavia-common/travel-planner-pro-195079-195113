# Frontend configuration
# By default the app calls the backend at http://localhost:3001/api
# Override for other environments (e.g. deployed backend URL)
REACT_APP_API_BASE_URL=http://localhost:3001/api
